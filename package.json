{
  "name": "awesome-utils",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "lerna run build",
    "lerna-init": "lerna init",
    "bootstrap": "pnpm install",
    "publish": "lerna publish",
    "utils-build": "lerna run build --scope=awesome-utils-owner",
    "ui-build": "lerna run build --scope=awesome-component-owner",
    "build-link": "lerna run build && lerna link",
    "clear": "lerna clean -y && rm -rf node_modules"
  },
  "files": [
    "dist"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
